const cds = require('@sap/cds')

module.exports = cds.service.impl (srv => {
  /*
  srv.after('READ', 'A_CustomerMaterial', (customerMaterial,req) => {
    console.log(customerMaterial)
    console.log(req.data)
  })
  */
})